# Financely Demo Site

Minimal static page with headings + two embeds (script and iframe).

## Deploy to Vercel

1. Import this repository in Vercel.
2. Set **Root Directory** to `demo-site`.
3. Framework preset: `Other`.
4. Build command: empty.
5. Output directory: empty (`.` static root).
6. Deploy.
