{
  "name": "financely-cloudflare-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for serving multi-tenant Financely brand sites",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:preview": "wrangler deploy --env preview"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240101.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.19.0"
  }
}

