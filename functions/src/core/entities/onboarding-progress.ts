import { OnboardingProgress as FrontendOnboardingProgress } from "../../../app/src/core/entities/onboarding-progress";

// Re-export the frontend entity type for backend use
export type OnboardingProgress = FrontendOnboardingProgress;
