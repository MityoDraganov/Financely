export enum DatabaseCollection {
  PROPOSALS = "proposals",
  INVOICES = "invoices",
  TEMPLATES = "templates",
  ORGANIZATIONS = "organizations",
  USERS = "users",
  WORKFLOWS = "workflows",
  BRAND_SITES = "brandSites",
  CONTACTS = "contacts",
}
